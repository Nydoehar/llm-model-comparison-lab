# llm-model-comparison-lab
llm-model-comparison-lab is a small Flask web app for comparing two Gemini models on the same prompt using a rubric-based evaluation workflow. It lets you generate side‑by‑side responses, score them across multiple quality dimensions, run a model-as-judge for automatic scoring, and analyze all results via a dashboard and CSV export.
